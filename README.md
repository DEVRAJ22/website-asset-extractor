# Extract pics workspace

The **Website Asset Extractor** MVP lives in [`website-asset-extractor`](./website-asset-extractor).

```bash
cd website-asset-extractor
npm install
npx playwright install chromium   # optional, for JS-rendered pages locally
npm run dev
```
